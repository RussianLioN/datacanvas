# PROC-062: Безопасный XLSX-Источник И Чистая История Git

ID: `PROC-062`
Статус: accepted
Автор: Codex
Дата: 2026-07-12
Целевая версия процесса: `0.1.0`

## Проблема

Исходный Excel Product Owner был добавлен в рабочую ветку вместе со служебным локальным путем внутри `xl/workbook.xml`. Действующие проверки находили локальные пути в зарегистрированных файлах текущего дерева, но не проверяли каждый XLSX во всех коммитах кандидата на слияние. Зелёный CI поэтому не доказывал безопасность истории ветки.

## Решение

- Оригинальный XLSX с закрытыми метаданными не хранится в Git; его SHA-256 сохраняется в машинном манифесте происхождения.
- Зарегистрированный генератор создаёт контролируемую копию и удаляет только namespace-independent элемент `absPath` из `xl/workbook.xml`.
- Побайтовые хэши всех частей OOXML доказывают, что других изменений исходного пакета нет.
- `npm run validate:git-history-hygiene` проверяет каждый коммит диапазона `origin/main..HEAD` и блокирует старый raw-путь, локальные указатели и внешние связи внутри XLSX.
- `npm run validate:ci-exact-sha` подтверждает, что CI проверяет точный SHA кандидата.
- Результат CI от предыдущего SHA после замены или принудительного обновления ветки считается недействительным.

## Граница Продуктового Смысла

Изменение не меняет Vision, BMC, пользовательские истории, бизнес-требования, критерии приемки, backlog, roadmap, приоритеты, сроки или ПШЕ DataCanvas. Допустимы только техническая очистка источника, обновление provenance, ссылок, каскадных контрактов и generated-навигации.

## Проверки

```bash
npm run test:source-security
npm run validate:xlsx-source-security
npm run validate:git-history-hygiene
npm run validate:ci-exact-sha
npm run validate:xlsx-backlog
npm run validate:xlsx-cascade
npm run validate:data-leakage
npm run validate:universal-documentation-workflow
npm test
```

## Миграция

Старый raw-путь удаляется из итогового дерева и всей истории нового кандидата. Контролируемый XLSX, манифест происхождения, рабочий XLSX и его provenance регистрируются как отдельные созависимые артефакты. Основной PR создаётся из новой ветки от чистого `main`; старый PR с загрязнённой историей закрывается без слияния.

## Rollback

До слияния закрыть чистый PR и не возвращать загрязнённую ветку как кандидат. После слияния откатывать merge отдельным PR. Нельзя откатывать изменение ослаблением leakage, history, exact-SHA или XLSX-проверок.

## Решение

Статус решения: accepted.
Дата решения: 2026-07-12.
Решающий владелец: пользователь, подтвердивший реализацию плана закрытия ревью, слияния и очистки веток DataCanvas.
