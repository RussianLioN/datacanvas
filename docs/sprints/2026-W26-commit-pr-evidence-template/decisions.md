# Decisions

- Template хранится в `docs/release/templates/commit-pr-evidence-template.md`.
- Validator проверяет, что release pack оставляет commit SHA pending.
- Нельзя считать локальный uncommitted worktree release evidence.
