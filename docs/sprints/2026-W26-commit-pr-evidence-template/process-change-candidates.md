# Process Change Candidates

- После первого PR решить, нужен ли отдельный machine-readable `release-train-evidence.json`.
- Рассмотреть CI check, который блокирует release acceptance без captured commit SHA.
