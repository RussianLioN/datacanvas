# Decisions

- Template artifacts хранятся в `docs/release/templates/`.
- Validator запрещает считать templates реальным pilot evidence.
- Real pilot files остаются внешним блокером до фактического pilot run.
