# Sprint Summary

Версия процесса: 0.1.0

Спринт добавил проверяемое версионирование процесса DataCanvas. Теперь версия `0.1.0` имеет manifest и snapshot, а validator проверяет, что все sprint evidence используют известную версию и включены в список применения этой версии.

Ограничение: создан только manifest для версии `0.1.0`; новая версия процесса должна добавляться отдельным Process Change Request.
