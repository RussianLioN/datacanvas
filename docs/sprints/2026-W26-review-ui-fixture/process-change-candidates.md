# Process Change Candidates: Review UI Fixture

## PCC-S30-001: Pilot Требует Real User Session

Кандидат: для pilot gate запретить `session_kind=fixture`; принимать только `session_kind=real_user` с фактическим actor_id и timestamps.
