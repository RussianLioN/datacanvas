# Process Change Candidates

Версия процесса: 0.1.0

## PROC-CANDIDATE-S9-001

Предложение: генерировать `provider-allowlist.yaml` из `provider-allowlist.json`.

Причина: сейчас YAML является человекочитаемой копией и может рассинхронизироваться.

Статус: candidate for retrospective.

## PROC-CANDIDATE-S9-002

Предложение: добавить `ProviderExperimentResult` schema до реального provider experiment.

Причина: результат эксперимента должен фиксировать качество, стоимость, latency, failure modes и rollback decision.

Статус: candidate for retrospective.
