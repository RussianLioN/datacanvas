# Sprint Goal: Contract Schemas

Версия процесса: 0.1.0

Закрыть gap по контрактам: `RenderRequest`, `ProcessChangeRequest`, `ToolAllowlist` и `TraceContract` должны иметь JSON Schema, fixtures и validation gate.
