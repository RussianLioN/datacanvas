# Artifact Updates

- `artifact-registry.schema.json` получил обязательные hash linkage fields.
- `artifact-registry.json` получил `hash_manifest_path`, `hash_algorithm`, `snapshot_policy`.
- `validate-artifact-registry.mjs` теперь проверяет hash manifest linkage и coverage.
- Plan coverage section 4 обновлен.
