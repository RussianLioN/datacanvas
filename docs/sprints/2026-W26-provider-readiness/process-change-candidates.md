# Process Change Candidates

Версия процесса: 0.1.0

## PROC-CANDIDATE-S8-001

Предложение: добавить JSON Schema для `provider-budget.json`.

Причина: бюджет provider должен быть валидируемым так же строго, как остальные machine-readable артефакты.

Статус: candidate for retrospective.

## PROC-CANDIDATE-S8-002

Предложение: добавить schema или structured parser для `provider-allowlist.yaml`.

Причина: текущая проверка allowlist структурно ограничена текстовым validator.

Статус: candidate for retrospective.
