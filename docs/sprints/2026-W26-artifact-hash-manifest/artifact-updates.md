# Artifact Updates

- Добавлены `ART-108` - `ART-113` в artifact registry.
- Добавлен `validate:artifact-hashes` в `package.json`.
- `generate:golden` теперь обновляет hash manifest после детерминированных генераторов.
- `docs-check` получил отдельный шаг `Check artifact hashes`.
- Plan coverage audit переведен к следующему gap: operational readiness.
